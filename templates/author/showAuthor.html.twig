<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Détails de l'auteur</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f9;
            margin: 0;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h1 {
            color: #333;
            text-align: center;
        }

        .author-details {
            background-color: white;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
            margin-top: 20px;
            width: 80%;
            max-width: 600px;
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        li {
            margin: 10px 0;
            font-size: 1.1em;
            color: #555;
        }

        li strong {
            color: #333;
        }

        img {
            border-radius: 5px;
            max-width: 100%;
            height: auto;
        }

        a {
            display: inline-block;
            margin-top: 20px;
            text-decoration: none;
            color: #4CAF50;
            font-weight: bold;
            padding: 10px 15px;
            border: 1px solid #4CAF50;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        a:hover {
            background-color: #4CAF50;
            color: white;
        }
    </style>
</head>
<body>
    {% if author is not defined or author is empty %}
        <h1>Auteur non trouvé</h1>
    {% else %}
        <div class="author-details">
            <h1>Détails de l'auteur</h1>
            <ul>
                <li><strong>Nom :</strong> {{ author.username }}</li>
                <li><strong>Email :</strong> {{ author.email }}</li>
                <li><strong>Photo :</strong> <img src="{{ asset(author.picture) }}" alt="Photo de {{ author.username }}"></li>
            </ul>
            <a href="{{ path('app_listAuthors') }}">Retour à la liste des auteurs</a>
        </div>
    {% endif %}
</body>
</html>
